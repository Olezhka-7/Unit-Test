using System;

namespace Unit_Test
{
    public class PowerTasks
    {
        public double Power(int a)
        {
            return Math.Pow(a, 2);
        }
        public int Sum (int c,int b)
        {
            return c + b;
        }
    }
}
